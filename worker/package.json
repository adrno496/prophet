{
  "name": "pulse-predict-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxying Groq for PULSE PREDICT freemium AI mode (rate-limited via KV).",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create QUOTA"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
